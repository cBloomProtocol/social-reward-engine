# ===========================================
# Social Reward Engine Configuration
# ===========================================

# Server
PORT=7200
NODE_ENV=development

# ===========================================
# MongoDB
# ===========================================
MONGODB_URI=mongodb://localhost:27017/social_reward_engine

# ===========================================
# X/Twitter API (Required)
# Get credentials from: https://developer.twitter.com
# ===========================================
X_API_BEARER_TOKEN=your_bearer_token_here
X_API_USER_ID=your_user_id_here

# ===========================================
# LLM Service & Scoring
# External LLM service for content scoring
# ===========================================
LLM_SERVICE_URL=https://llm.example.com
LLM_API_KEY=llm_sk_xxx
# Preferred LLM Provider (will fallback to others if unavailable)
# Options: anthropic | openai | deepseek | gemini
LLM_PROVIDER=anthropic
# Delay between scoring each post (ms) to avoid rate limiting
SCORER_ITEM_DELAY=1000

# ===========================================
# X402 Payment (Optional)
# Blockchain payment via X402 protocol with CDP
# ===========================================
# URL of your deployed X402 Worker (Cloudflare Workers)
X402_WORKER_URL=https://your-x402-worker.workers.dev
# Network: 'base' (EVM) or 'solana'
X402_NETWORK=base
# EVM private key for Base network (WARNING: Never commit!)
X402_EVM_PRIVATE_KEY=
# Solana private key (base58 encoded, WARNING: Never commit!)
# X402_SOLANA_PRIVATE_KEY=

# ===========================================
# Fetcher & Cron Configuration
# ===========================================
# Max age of posts to fetch (in days, default: 90)
FETCH_MAX_AGE_DAYS=90
# Cron schedules (default: fetch */5, score */5, payout */10)
# CRON_FETCH=*/5 * * * *
# CRON_SCORE=*/5 * * * *
# CRON_PAYOUT=*/10 * * * *
